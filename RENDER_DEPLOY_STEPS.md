# 🚀 خطوات نشر MedFutureAI على Render

## 📋 ما تم إعداده

تم تحويل المشروع من SQLite إلى PostgreSQL وتجهيزه للعمل مع Supabase. جميع الملفات المطلوبة موجودة في المشروع.

## 📝 خطوات النشر

### الخطوة 1: إنشاء حساب على Render

1. افتح [https://render.com](https://render.com)
2. سجل الدخول باستخدام حساب GitHub الخاص بك
3. ستتم إعادة توجيهك إلى لوحة التحكم

### الخطوة 2: إنشاء Web Service

1. في لوحة التحكم، اضغط على **New +** في الأعلى
2. اختر **Web Service**
3. اختر **GitHub** كمصدر
4. اختر مستودع `mwr0855-rgb/MedFutureAI`
5. اختر الفرع `main`
6. اختر ملف التكوين `render.yaml`

### الخطوة 3: إعداد Environment Variables

بعد إنشاء الخدمة، اذهب إلى **Environment** وقم بإضافة المتغيرات التالية:

#### للـ Backend:

```env
NODE_ENV=production
PORT=3001
DATABASE_URL=postgresql://username:password@host:port/database
ADMIN_USERNAME=FutureMed_AmrX
ADMIN_PASSWORD=Fm@2045!MedX
CORS_ORIGIN=https://your-frontend-domain.onrender.com
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
```

**ملاحظات مهمة:**
- استبدل `DATABASE_URL` بـ Connection String من Supabase
- استبدل `CORS_ORIGIN` بعنوان Frontend الفعلي
- **لا تستخدم كلمة المرور الافتراضية في الإنتاج!** غيّرها فوراً

#### للـ Frontend:

```env
VITE_API_URL=https://your-backend-domain.onrender.com/api
```

**ملاحظة:** استبدل `your-backend-domain.onrender.com` بعنوان Backend الفعلي.

### الخطوة 4: بدء النشر

بعد إضافة جميع المتغيرات البيئية، اضغط على **Create Web Service**. Render سيبدأ تلقائياً في بناء ونشر التطبيق.

### الخطوة 5: التحقق من النشر

بعد اكتمال النشر، ستحصل على عنوانين:

- **Frontend:** `https://your-frontend-domain.onrender.com`
- **Backend:** `https://your-backend-domain.onrender.com`

يمكنك التحقق من صحة backend عن طريق زيارة:
```
https://your-backend-domain.onrender.com/api/health
```

يجب أن ترى:
```json
{
  "success": true,
  "message": "Backend is running",
  "timestamp": "2025-12-11T10:00:00.000Z"
}
```

## 📝 خطوات إنشاء قاعدة البيانات على Supabase

### الخطوة 1: الحصول على Connection String

1. افتح لوحة تحكم Supabase: [https://supabase.com/dashboard](https://supabase.com/dashboard)
2. اختر مشروعك: `bzibmjooqgfobdmtzyxv`
3. اذهب إلى **Settings → Database**
4. انسخ **Connection String**

### الخطوة 2: إنشاء الجداول

#### الطريقة 1: استخدام SQL Editor (الأسهل)

1. في لوحة تحكم Supabase، اذهب إلى **SQL Editor**
2. انسخ محتويات ملف `backend/schema-postgres.sql`
3. الصق الكود في SQL Editor
4. اضغط على **Run**

#### الطريقة 2: استخدام GitHub

1. افتح ملف `backend/schema-postgres.sql` في GitHub
2. انسخ المحتوى
3. الصق في SQL Editor في Supabase
4. اضغط على **Run**

### الخطوة 3: إنشاء حساب الأدمن

بعد إنشاء الجداول، يجب إنشاء حساب الأدمن الافتراضي:

```sql
-- إنشاء حساب الأدمن
INSERT INTO admins (username, password_hash) 
VALUES ('FutureMed_AmrX', '$2b$10$YourHashedPasswordHere')
ON CONFLICT (username) DO NOTHING;
```

**ملاحظة مهمة:** يجب تشفير كلمة المرور باستخدام bcrypt. يمكنك استخدام أداة عبر الإنترنت مثل [bcrypt-generator](https://bcrypt-generator.com/) لتوليد الهاش.

## 🎯 ما بعد النشر

### تغيير كلمة مرور الأدمن

بعد النشر، **يجب** تغيير كلمة المرور الافتراضية فوراً:

1. سجل الدخول إلى لوحة تحكم الأدمن
2. اذهب إلى إعدادات الحساب
3. غيّر كلمة المرور إلى كلمة مرور قوية
4. حدّث متغير `ADMIN_PASSWORD` في Render Environment

### اختبار التطبيق

1. افتح عنوان Frontend
2. اختبر جميع الوظائف
3. تحقق من أن الاتصال بـ Backend يعمل
4. تحقق من أن قاعدة البيانات تعمل بشكل صحيح

## 📊 المراقبة

### Render Dashboard

Render يوفر لوحة تحكم شاملة:

- **Health Checks:** تحقق تلقائي من صحة الخدمة
- **Logs:** عرض logs في الوقت الفعلي
- **Metrics:** مقاييس الأداء الأساسية

### Supabase Dashboard

Supabase يوفر أيضاً مراقبة شاملة:

- **Database Activity:** مراقبة استعلامات قاعدة البيانات
- **Performance:** تحليل أداء الاستعلامات
- **Alerts:** إشعارات عند حدوث مشاكل

## 🔧 الصيانة

### تحديث الكود

عندما تقوم بتحديث الكود على GitHub، سيقوم Render تلقائياً بإعادة النشر إذا كان "Auto Deploy" مفعلاً.

### إعادة النشر يدوياً

1. اذهب إلى خدمة Render
2. اضغط على **Deploy**
3. اختر **Deploy Latest Commit**

### التراجع عن التحديث

إذا حدثت مشكلة:

```bash
git checkout <commit-hash>
git push origin main
```

Render سيقوم تلقائياً بإعادة النشر بالنسخة القديمة.

## 💰 التكاليف

### Render

- **Web Services:** 100 ساعات شهرياً لكل خدمة (مجاني)
- **Static Sites:** غير محدود (مجاني)

### Supabase

- **Free Tier:** 500 MB Database + 10 GB Bandwidth (مجاني)
- **Pro Tier:** 8 GB Database + 100 GB Bandwidth ($25/شهر)

للمشروع الحالي، ستكون التكاليف صفرية طالما لا تتجاوز الاستخدامات الخطة المجانية.

## 🆘 الدعم

### Render Documentation

- [https://render.com/docs](https://render.com/docs)
- [https://community.render.com](https://community.render.com)

### Supabase Documentation

- [https://supabase.com/docs](https://supabase.com/docs)
- [https://supabase.com/docs/guides/database](https://supabase.com/docs/guides/database)

### GitHub Issues

- [https://github.com/mwr0855-rgb/MedFutureAI/issues](https://github.com/mwr0855-rgb/MedFutureAI/issues)

## ✅ قائمة التحقق

قبل النشر، تأكد من:

- [ ] رفع جميع التغييرات إلى GitHub
- [ ] إنشاء حساب Render
- [ ] ربط GitHub repo
- [ ] الحصول على Connection String من Supabase
- [ ] إنشاء الجداول على Supabase
- [ ] إنشاء حساب الأدمن
- [ ] إضافة Environment Variables
- [ ] تغيير كلمة مرور الأدمن الافتراضية
- [ ] اختبار جميع الوظائف
- [ ] إعداد المراقبة والإشعارات

## 🎉 التهاني!

بعد اتباع هذه الخطوات، سيكون مشروع MedFutureAI منشوراً بنجاح على Render مع قاعدة بيانات Supabase المدارة. يمكنك الآن الوصول إلى التطبيق من أي مكان في العالم!

---

**مطور المشروع:** Amr AI  
**الإصدار:** 1.0  
**التاريخ:** 2025-12-11  
**المنصة:** Render + Supabase
