# 🚀 دليل النشر على Render - MedFutureAI

## 📋 ملخص المشروع

مشروع MedFutureAI هو نظام طبي ذكي يعتمد على الذكاء الاصطناعي. يتكون المشروع من جزأين رئيسيين: واجهة مستخدم React (Frontend) وخادم Node.js (Backend) مع قاعدة بيانات.

## 🎯 الأهداف

نشر المشروع على منصة Render مع ربطه بقاعدة بيانات PostgreSQL مدارة.

## 📦 التغييرات المطلوبة

لنشر المشروع على Render، تم إجراء التعديلات التالية:

### 1. قاعدة البيانات

تم تحويل نظام قاعدة البيانات من SQLite إلى PostgreSQL، وهي قاعدة بيانات مدارة توفرها Render مجاناً. التغييرات تشمل:

- إنشاء ملف `database-postgres.js` الجديد الذي يستخدم `pg` كـ PostgreSQL driver
- تحويل ملف `schema.sql` إلى `schema-postgres.sql` مع تعديلات ملائمة لـ PostgreSQL
- تحديث `server.js` إلى `server-postgres.js` لاستخدام قاعدة البيانات الجديدة
- إضافة `pg` كتبعية في `package.json` وإزالة `better-sqlite3`

### 2. ملفات التكوين

- إنشاء ملف `render.yaml` الذي يحدد خدمات Render الثلاثة (قاعدة البيانات، backend، frontend)
- تحديث `package.json` في مجلد backend لاستخدام الملفات الجديدة
- إنشاء ملف `init-postgres-db.js` لتهيئة قاعدة البيانات

## 📝 خطوات النشر

### الخطوة 1: رفع التغييرات إلى GitHub

قبل البدء في النشر، يجب رفع جميع التعديلات إلى GitHub:

```bash
# الانتقال إلى مجلد المشروع
cd /home/ubuntu/MedFutureAI

# إضافة جميع الملفات الجديدة
git add .

# إنشاء commit
git commit -m "feat: Prepare for Render deployment with PostgreSQL"

# رفع التغييرات إلى GitHub
git push origin main
```

### الخطوة 2: إنشاء حساب على Render

إذا لم يكن لديك حساب على Render بعد، قم بإنشاء واحد من خلال الموقع الرسمي: [https://render.com](https://render.com)

### الخطوة 3: ربط GitHub repo

1. افتح لوحة تحكم Render
2. اضغط على "New +" في الأعلى
3. اختر "Web Service"
4. اختر "GitHub" كمصدر
5. اختر مستودع `mwr0855-rgb/MedFutureAI`
6. اختر الفرع `main`

### الخطوة 4: إعداد قاعدة البيانات

Render سيقوم تلقائياً بإنشاء قاعدة بيانات PostgreSQL بناءً على ملف `render.yaml`. لا حاجة لإجراء أي إجراءات إضافية.

### الخطوة 5: إعداد Environment Variables

بعد إنشاء الخدمة، ستحتاج إلى إضافة المتغيرات البيئية التالية:

#### للـ Backend:

```env
NODE_ENV=production
PORT=3001
ADMIN_USERNAME=FutureMed_AmrX
ADMIN_PASSWORD=Fm@2045!MedX
CORS_ORIGIN=https://your-frontend-domain.onrender.com
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
```

**ملاحظة مهمة:** يجب استبدال `your-frontend-domain.onrender.com` بعنوان Frontend الفعلي الذي ستحصل عليه بعد النشر.

#### للـ Frontend:

```env
VITE_API_URL=https://your-backend-domain.onrender.com/api
```

**ملاحظة مهمة:** يجب استبدال `your-backend-domain.onrender.com` بعنوان Backend الفعلي الذي ستحصل عليه بعد النشر.

### الخطوة 6: بدء النشر

بعد إضافة جميع المتغيرات البيئية، اضغط على "Create Web Service". Render سيبدأ تلقائياً في بناء ونشر التطبيق.

### الخطوة 7: تهيئة قاعدة البيانات

بعد نشر backend بنجاح، ستحتاج إلى تشغيل سكريبت تهيئة قاعدة البيانات:

1. افتح لوحة تحكم Render
2. اذهب إلى خدمة backend
3. اضغط على "Shell" أو "Console"
4. اكتب الأمر التالي:

```bash
cd backend && npm run init-postgres-db
```

هذا سيقوم بإنشاء الجداول وإضافة حساب الأدمن الافتراضي.

## 🌐 الوصول إلى التطبيق

بعد اكتمال النشر، ستحصل على عنوانين:

- **Frontend:** `https://your-frontend-domain.onrender.com`
- **Backend:** `https://your-backend-domain.onrender.com`

يمكنك الوصول إلى واجهة المستخدم من خلال عنوان Frontend، وسيتم ربطه تلقائياً بـ Backend.

## 🔐 الأمان

### تغيير كلمة مرور الأدمن

بعد النشر، يجب تغيير كلمة مرور الأدمن الافتراضية فوراً:

1. سجل الدخول إلى لوحة تحكم الأدمن
2. اذهب إلى إعدادات الحساب
3. غيّر كلمة المرور إلى كلمة مرور قوية

### تفعيل HTTPS

Render يوفر HTTPS تلقائياً لجميع الخدمات، لذا لا حاجة لإجراء أي إجراءات إضافية.

## 📊 المراقبة

### مراقبة الحالة

Render يوفر لوحة تحكم شاملة لمراقبة حالة الخدمات:

- **Health Checks:** Render يقوم تلقائياً بالتحقق من صحة الخدمة من خلال endpoint `/api/health`
- **Logs:** يمكنك عرض logs في الوقت الفعلي من لوحة التحكم
- **Metrics:** Render يوفر مقاييس الأداء الأساسية

### إعداد إشعارات

يمكنك إعداد إشعارات البريد الإلكتروني عند حدوث مشاكل:

1. اذهب إلى إعدادات الخدمة
2. اختر "Alerts"
3. أضف عنوان بريدك الإلكتروني
4. اختر أنواع الإشعارات التي تريدها

## 🔧 الصيانة

### تحديث الكود

عندما تقوم بتحديث الكود على GitHub، سيقوم Render تلقائياً بإعادة النشر إذا كان "Auto Deploy" مفعلاً.

### إعادة النشر يدوياً

إذا كنت تريد إعادة النشر يدوياً:

1. اذهب إلى خدمة Render
2. اضغط على "Deploy"
3. اختر "Deploy Latest Commit"

### التراجع عن التحديث

إذا حدثت مشكلة بعد تحديث:

1. اذهب إلى GitHub repo
2. ابحث عن commit السابق
3. قم بالرجوع إليه باستخدام:

```bash
git checkout <commit-hash>
git push origin main
```

Render سيقوم تلقائياً بإعادة النشر بالنسخة القديمة.

## 💰 التكاليف

Render يوفر خطة مجانية تشمل:

- **Web Services:** 100 ساعات شهرياً لكل خدمة
- **PostgreSQL Database:** 10,000 rows (مجاني)
- **Static Sites:** غير محدود

للمشروع الحالي، ستكون التكاليف صفرية طالما لا تتجاوز الاستخدامات الخطة المجانية.

## 🆘 الدعم

### المشاكل الشائعة

#### 1. خطأ في الاتصال بقاعدة البيانات

تأكد من:
- أن قاعدة البيانات قد تم إنشاؤها بنجاح
- أن متغير `DATABASE_URL` موجود في Environment Variables
- أن backend يحاول الاتصال بالقاعدة الصحيحة

#### 2. Frontend لا يظهر

تأكد من:
- أن متغير `VITE_API_URL` يشير إلى عنوان Backend الصحيح
- أن Frontend قد تم بناؤه بنجاح (تحقق من logs)
- أن ملفات `dist` موجودة

#### 3. Backend لا يستجيب

تأكد من:
- أن متغير `PORT` مضبوط على 3001
- أن endpoint `/api/health` يعمل
- أن قاعدة البيانات قد تم تهيئة بنجاح

### مصادر الدعم

- **Render Documentation:** [https://render.com/docs](https://render.com/docs)
- **Render Community:** [https://community.render.com](https://community.render.com)
- **GitHub Issues:** [https://github.com/mwr0855-rgb/MedFutureAI/issues](https://github.com/mwr0855-rgb/MedFutureAI/issues)

## 📝 ملاحظات إضافية

### التحسينات المستقبلية

عندما ينمو المشروع، يمكنك التفكير في:

1. **ترقية الخطة:** لزيادة عدد ساعات الاستخدام
2. **إضافة CDN:** لتسريع تحميل الملفات الثابتة
3. **استخدام Redis:** للـ caching وتحسين الأداء
4. **Database Replication:** لزيادة الموثوقية

### النسخ الاحتياطي

Render يوفر نسخ احتياطية تلقائية لقاعدة البيانات. يمكنك أيضاً إعداد نسخ احتياطية يدوية:

```bash
# تصدير قاعدة البيانات
pg_dump -h <host> -U <user> -d <database> > backup.sql

# استيراد قاعدة البيانات
psql -h <host> -U <user> -d <database> < backup.sql
```

## ✅ قائمة التحقق

قبل النشر، تأكد من:

- [ ] رفع جميع التغييرات إلى GitHub
- [ ] إنشاء حساب Render
- [ ] ربط GitHub repo
- [ ] إضافة Environment Variables
- [ ] تهيئة قاعدة البيانات
- [ ] تغيير كلمة مرور الأدمن الافتراضية
- [ ] اختبار جميع الوظائف
- [ ] إعداد المراقبة والإشعارات

## 🎉 التهاني!

بعد اتباع هذه الخطوات، سيكون مشروع MedFutureAI منشوراً بنجاح على Render مع قاعدة بيانات PostgreSQL مدارة. يمكنك الآن الوصول إلى التطبيق من أي مكان في العالم!

---

**مطور المشروع:** Amr AI  
**الإصدار:** 1.0  
**التاريخ:** 2025-12-11
